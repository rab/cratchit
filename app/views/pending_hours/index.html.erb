<h1>Listing pending_hours</h1>

<table>
  <tr>
    <th>Role</th>
    <th>Start date</th>
    <th>End date</th>
    <th>Hours</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pending_hours.each do |pending_hour| %>
  <tr>
    <td><%= pending_hour.role %></td>
    <td><%= pending_hour.start_date %></td>
    <td><%= pending_hour.end_date %></td>
    <td><%= pending_hour.hours %></td>
    <td><%= link_to 'Show', pending_hour %></td>
    <td><%= link_to 'Edit', edit_pending_hour_path(pending_hour) %></td>
    <td><%= link_to 'Destroy', pending_hour, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pending hour', new_pending_hour_path %>
